/**
 * Atenção tem que ter os mesmos atributos que no backend
 * ? significa que é um atributo opcional
 */
export interface SuperficieSuporte {
    id: string;
    descricao: string;
    score: number;
}